# Modelos 3D

Coloca aquí el archivo:

- `pieza.glb` — export desde Spline / Meshy / Tripo con **compresión Draco**

El hero lo detecta solo (`HEAD /models/pieza.glb`). Mientras no exista, usa el poster `public/images/hero/fallback-3d.png` texturizado en el canvas.
